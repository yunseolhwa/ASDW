# Experimental Windows provider candidates.
# Not part of the minimum baseline stack.
# Do not install by default; use only for a focused provider experiment.
pywinctl==0.4.1
pywinauto==0.6.9
pydirectinput-rgx==2.1.3
dxcam==0.3.0

# Future HWND/HMONITOR capture candidate:
# windows-capture
